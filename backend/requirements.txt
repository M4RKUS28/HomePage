# ── Web Framework ──
fastapi>=0.115
uvicorn[standard]>=0.30

# ── Database (PostgreSQL + async) ──
sqlalchemy[asyncio]>=2.0
asyncpg                    # async PostgreSQL driver
psycopg2-binary            # sync driver (used by Alembic, self-contained binary)
alembic>=1.13

# ── Redis ──
redis>=5.0                 # redis.asyncio (async Redis client)

# ── Object Storage ──
minio>=7.2

# ── Auth & Security ──
python-jose[cryptography]
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# ── Validation & Settings ──
pydantic[email]>=2.0
pydantic-settings>=2.0
email-validator

# ── Async HTTP (health checks) ──
httpx>=0.27

# ── Async Email ──
aiosmtplib>=3.0

# ── Background Scheduling ──
apscheduler>=3.10

# ── AI Translation (Google Gemini) ──
google-adk~=1.26.0

# ── Utilities ──
python-dotenv
python-multipart           # file uploads in FastAPI
